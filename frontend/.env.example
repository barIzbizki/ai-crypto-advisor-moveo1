# Copy to .env for local development. All values below are safe local defaults.

# Base URL the API client sends requests to. Matches the local backend's
# default port (see backend/.env.example).
VITE_API_BASE_URL=http://localhost:3000
COINGECKO_API_KEY=